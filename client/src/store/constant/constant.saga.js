import { takeLatest, put, all, call } from 'redux-saga/effects';
import { cons } from './constant.actiontype';
import { actions } from './constant.action';

export function* constantSaga() {
  yield all([
  ]);
}